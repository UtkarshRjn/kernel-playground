{
  "name": "kernel-playground",
  "version": "0.0.0",
  "private": true,
  "description": "Colab for kernel code — write GPU kernels and compare the same kernel across multiple GPUs.",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter @kp/web dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
